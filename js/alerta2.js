// Función que se ejecuta al hacer clic en el botón para continuar
function confirmarCobro() {
    // Muestra un aviso personalizado
    var confirmationMessage = '¿Estás seguro de continuar con el proceso de cobro? Esta acción no se puede revertir.';
    return confirm(confirmationMessage);
}
